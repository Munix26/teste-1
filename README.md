# teste-1

Repositório reiniciado para um novo site HTML (escopo ainda a definir).

## Estado atual

- `index.html` — placeholder "em construção".
- `.mcp.json` — configuração do servidor MCP do Tibia para uso com Claude Code.

## MCP

O projeto usa o servidor MCP em `https://tibia-mcp.vercel.app`. A configuração
em `.mcp.json` aponta para o endpoint `/mcp`; se o servidor expuser o endpoint
em outro caminho (ex.: `/api/mcp` ou `/sse`), ajuste a URL nesse arquivo.
